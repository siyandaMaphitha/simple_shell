#include <stdio.h>
#include <stdlib.h>

/**
 * main- prints all arguments without using ac
 * @ac: number of arguments in ac
 * @av: array of strings (arguments)
 * 
 * Return: o Success
 */

int main(int ac, char **av)
{
	int a = o; 

	for(a = 0; a < ac; a++)
	{
		(void) ac;
		printf("%s\n", av[a]);
	}
	return (0);
}
